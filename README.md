# Микросервисная система отправки email

Проект демонстрирует архитектуру из двух микросервисов, взаимодействующих через **Apache Kafka**, с централизованным обнаружением сервисов через **Eureka Server**.  
Пользователь отправляет email и текст через веб-интерфейс → сообщение асинхронно передаётся в сервис отправки почты.

## 🧩 Архитектура

- **`eureka-server`** — сервер обнаружения сервисов (Service Discovery).
- **`message-receiver-client`** — веб-сервис с UI-формой, принимает сообщения и отправляет их в Kafka.
- **`email-service`** — слушает Kafka и отправляет email через SMTP.
